import styled from 'styled-components'

const Wrapper = styled.div `
  padding: 10px 0;
  font-size: 17px;
`

const HightLight = styled.span `
  font-style: italic;
  font-size: 19px;
  font-weight: bold;
`

export { Wrapper, HightLight }
