// import React from 'react';
// import { shallow } from 'enzyme';

// import HighlightResultTable from '../index';

describe('<HighlightResultTable />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
