import { defineMessages } from 'react-intl'

export default defineMessages({
  default: {
    id: 'app.containers.FormSearchInput.SpecializationSelect.default',
    defaultMessage: 'Specialization',
  },
})
